Week 5: Functional Programming (Advanced Thinking)
มีการฝึกเขียน function โดยนำ arr Methods มาประยุกต์ใช้ในการเขียน โจทย์ที่มีระดับความยากที่เพิ่มขึ้น
//passingScore
มีการเก็บข้อมูลในรูปแบบ obj ไว้ใน arr
มีการเลือกใช้ studentScore.filter ในการหานักเรียนที่มีคะแนนสอบมากกว่า หรือ เท่ากับ 70
.map(student => student.name) และมีการใช้ .map ในการเลือกแสดงผลข้อมูลนักเรียนที่ผ่านเกณฑ์ด้วยชื่อ

//merge string
มีการรวม arr ทั้ง 2 arr ด้วยการใช้คำสั่ง .concat ในการรวม arr และใช้ .join ในการรวม arr ด้วยช่องว่าง

//reverse
.reverse ในการจัดลำดับการแสดงผลข้อมูลใหม่ โดยเรียงจากข้างหลัง -> มาข้างหน้า

//sort การแปลงค่าเป็น string และเรียงลำดับตามพจนานุกรม
.sort ใช้ในการเรียงลำดับ array ภายใต้เงื่อนไขว่า -> (a,b) => a-b;
ความหมายคือ return > 0 = a ต้องมาก่อน B || return < 0 = b ต้องมาก่อน a

